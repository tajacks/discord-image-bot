{
  "name": "draw",
  "description": "Draw an image with the given prompt.",
  "options": [
    {
      "name": "prompt",
      "description": "The prompt to be drawn",
      "type": 3,
      "required": true
    }
  ]
}